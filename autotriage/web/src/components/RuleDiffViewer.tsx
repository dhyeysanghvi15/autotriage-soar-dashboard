import React from "react";

export default function RuleDiffViewer() {
  return (
    <div className="muted">
      Replay experiments support config overrides; this view can be extended to show a rendered diff of YAML
      weights and thresholds.
    </div>
  );
}

