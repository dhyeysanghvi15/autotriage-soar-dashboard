services:
  autotriage:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    ports:
      - "8080:8080"
    environment:
      - AUTOTRIAGE_DB_PATH=/app/var/autotriage.db
    volumes:
      - autotriage_var:/app/var
volumes:
  autotriage_var: {}

